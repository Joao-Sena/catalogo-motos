name: Continuous Integration

on:
  pull_request:
  push:

jobs:
  continuous-integration:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Using Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 22.x

      - name: run install, build, lint and test
        run: |
          npm install
          npm run lint
          sudo apt-get update
          sudo apt-get install -y xvfb
          xvfb-run -a npm test -- --watch=false --browsers=ChromeHeadless --progress=false
          npm run build

  exemplo-job:
  name: Pegar o Run Id da Action
  runs-on: ubuntu-latest
  steps:
    - name: Pegar o Run ID
      run: echo "O ID desta execução é ${{ github.run_id }}"

  create-pr-develop-to-main:
    name: Create PR develop -> main
    runs-on: ubuntu-latest
    needs: continuous-integration
    if: ${{ success() && github.event_name == 'push' && github.ref == 'refs/heads/develop' }}

    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Create or update PR
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;

            const headBranch = "develop";
            const baseBranch = "main";

            // Busca PR existente entre develop -> main (caso já exista, não cria outro)
            const { data: existingPrs } = await github.rest.pulls.list({
              owner,
              repo,
              state: "open",
              head: `${owner}:${headBranch}`,
              base: baseBranch,
              per_page: 100
            });

            if (existingPrs.length > 0) {
              core.info(`PR já existe (#${existingPrs[0].number}). Nenhuma ação necessária.`);
              return;
            }

            await github.rest.pulls.create({
              owner,
              repo,
              title: "Automated PR: develop -> main",
              head: headBranch,
              base: baseBranch,
              body: "Este PR foi criado automaticamente após o pipeline de CI passar no branch develop."
            });
